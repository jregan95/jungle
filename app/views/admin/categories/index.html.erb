<h1> Categories </h1>
<ul>
 <% @categories.each do |category| %> 
  <li> <%= category %> </li>
<% end %>
</ul>

<div class="well">
    <%= link_to '+ New Category', [:new, :admin, :category], class: 'btn btn-info' %>
  </div>